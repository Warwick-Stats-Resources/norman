# Print and return a table of module effects

Print and return a table of module effects

## Usage

``` r
get_module_effects(module_codes, mdd)
```

## Arguments

- module_codes:

  Character; the vector of module codes used in the report

- mdd:

  List of median differences

## Value

A 2-column data frame with columns `Effect` and `Count`
